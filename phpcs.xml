<?xml version="1.0"?>
<ruleset name="BeeSwarm Clean Code"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:noNamespaceSchemaLocation="vendor/squizlabs/php_codesniffer/phpcs.xsd">

    <description>PSR-12 + Clean Code principles for bee_swarm</description>

    <!-- PSR-12 baseline -->
    <rule ref="PSR12"/>

    <!-- Clean Code: cognitive load -->
    <rule ref="Generic.Metrics.CyclomaticComplexity">
        <properties>
            <property name="complexity" value="10"/>
            <property name="absoluteComplexity" value="20"/>
        </properties>
    </rule>
    <rule ref="Generic.Metrics.NestingLevel">
        <properties>
            <property name="absoluteNestingLevel" value="4"/>
        </properties>
    </rule>

    <!-- Clean Code: method/class size -->
    <rule ref="Generic.Files.LineLength">
        <properties>
            <property name="lineLimit" value="120"/>
            <property name="absoluteLineLimit" value="200"/>
        </properties>
    </rule>

    <!-- Clean Code: no else after return -->
    <rule ref="Generic.CodeAnalysis.EmptyStatement"/>
    <rule ref="Squiz.PHP.NonExecutableCode"/>

    <!-- Exclude tests from some rules -->
    <rule ref="PSR1.Files.SideEffects">
        <exclude-pattern>*/tests/*</exclude-pattern>
    </rule>
</ruleset>
